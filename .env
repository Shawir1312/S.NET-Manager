# ================================================================
# S.NET Manager v2.1 — Environment Configuration
# ================================================================
# JANGAN upload file ini ke server publik atau commit ke git!
# ================================================================

# Database
DB_HOST=localhost
DB_NAME=snet1
DB_USER=snet1
DB_PASS=snet1

# App Secret (gunakan string acak minimal 32 karakter)
# Generate: php -r "echo bin2hex(random_bytes(32));"
APP_SECRET=ganti_dengan_string_acak_minimal_32_karakter_disini

# GenieACS SSL Verification (true = aktifkan verifikasi SSL, false = nonaktifkan)
GENIE_SSL_VERIFY=false
